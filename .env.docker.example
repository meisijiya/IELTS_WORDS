# Override these in production!
POSTGRES_USER=yasi
POSTGRES_PASSWORD=yasi_dev_pwd_change_me
POSTGRES_DB=yasi_db
ADMIN_PASSWORD=yasi-2026-dev
SESSION_SECRET=change-me-in-production-min-32-chars-long
APP_PORT=3000
TZ=Asia/Shanghai

# Bake pronunciation audio into the Docker image at build time (238 MB).
# Update the tag when uploading a new audio bundle (see tools/release-audio.sh).
# Leave empty to ship image without audio (you'll need to fetch manually).
AUDIO_BUNDLE_URL=https://github.com/meisijiya/IELTS_WORDS/releases/download/audio-2026-07-21/audio.tgz